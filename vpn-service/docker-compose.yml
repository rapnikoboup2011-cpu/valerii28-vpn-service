services:
    vpn-bot:
        build: .
        container_name: vpn-bot
        restart: always
        env_file:
            - .env
        ports:
            - '127.0.0.1:8080:8080'
        volumes:
            - vpn-bot-data:/app/data

volumes:
    vpn-bot-data:
        driver: local
